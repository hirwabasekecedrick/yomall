import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Deal Moderation",
};

export default function DealModerationLayout({ children }: { children: React.ReactNode }) {
  return children;
}
