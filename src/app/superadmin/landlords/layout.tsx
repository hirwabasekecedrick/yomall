import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Landlords & Malls",
};

export default function LandlordsMallsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
