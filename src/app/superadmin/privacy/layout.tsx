import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Privacy & Data",
};

export default function PrivacyDataLayout({ children }: { children: React.ReactNode }) {
  return children;
}
