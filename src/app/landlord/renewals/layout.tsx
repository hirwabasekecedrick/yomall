import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Lease Renewals",
};

export default function LeaseRenewalsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
