import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "yoDeals Deals",
};

export default function yoDealsDealsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
